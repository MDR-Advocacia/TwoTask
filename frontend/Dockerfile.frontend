# Conteúdo para: /frontend/Dockerfile.frontend

# Use uma imagem Node.js moderna
FROM node:20-alpine

# Define o diretório de trabalho
WORKDIR /app

# Copia os arquivos de manifesto de pacotes
COPY package*.json ./

# Instala as dependências
RUN npm install

# Copia todo o resto do código do frontend
COPY . .

# Expõe a porta do Vite
EXPOSE 8080

# O comando para iniciar o servidor de desenvolvimento
CMD ["npm", "run", "dev"]